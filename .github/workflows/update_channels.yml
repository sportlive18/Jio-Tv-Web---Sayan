name: Update Channels (Auto Sync MPD & Keys)

on:
  schedule:
    - cron: '0 */6 * * *' # Checks every 6 hours
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Download external M3U
        run: |
          echo "Downloading latest jtv.m3u..."
          curl -fSL "https://raw.githubusercontent.com/alex4528x/m3u_1/refs/heads/main/jtv.m3u" -o jtv.m3u
          echo "Download complete. File size: $(wc -c < jtv.m3u) bytes"

      - name: Generate channel HTML files
        run: python generate_channels.py

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet && git diff --cached --quiet && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
          git commit -m "🔄 Auto-update channels: MPD & keys synced ($TIMESTAMP)"
          git push
